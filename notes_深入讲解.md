# 自迭代模型 —— 深入讲解笔记

这份笔记系统整理"自迭代模型"（Self-Improving / Self-Evolving Model）这一研究方向的核心概念、代表性方法和前沿进展，内容深度超过配套幻灯片，覆盖具体的算法机制、数学推导和技术细节。领域特有的术语（few-shot、RLHF、DPO等）会在第一次出现的地方就地解释，后续章节复用时会简单提一句"回顾第X节"，不重复展开整段。

---

## 预备知识：神经网络训练的几个基本概念

这一节讲最基础的机器学习背景，供完全不熟悉神经网络训练流程的读者参考；如果已经了解"模型/参数/损失函数/梯度下降/监督学习"这些概念，可以直接跳到第0节。

**神经网络/模型**：一个由大量可调节数值（叫"参数"或"权重"）组成的数学函数，输入一些数据，经过一层层计算，输出一个结果。"训练"一个模型，指的就是不断调整这些参数的数值，使得模型的输出越来越接近期望的结果。参数的数量可以从几千到几千亿不等（"大模型"通常指参数量在十亿到万亿级别的模型）。

**监督学习(supervised learning)**：最常见的训练方式——准备一批"输入-正确答案"的配对数据（叫"标注数据"，正确答案叫"标签"），把输入喂给模型，比较模型输出和标签之间的差距，用这个差距去调整参数，让模型下次输出更接近标签。比如"给一张猫的图片，标签是'猫'"就是一条监督学习样本。

**损失函数(loss function)**：一个用来量化"模型输出和期望结果差多远"的数学公式，差距越大，损失函数算出的数值越大。训练的目标就是通过调整参数，使损失函数的值尽量小。

**梯度下降(gradient descent)与反向传播(backpropagation)**：调整参数的具体方法。"梯度"是损失函数对每个参数的偏导数，指出了"如果把这个参数往哪个方向调整一点点，损失会怎么变化"；梯度下降就是让每个参数都往"能让损失变小"的方向移动一小步，反复迭代很多次，损失逐渐降低，模型效果逐渐变好。"反向传播"是神经网络里高效计算这些梯度的具体算法，是深度学习训练的标准底层机制，这份笔记里遇到"梯度"、"求导"、"更新参数"这类说法，指的都是这套流程。

**强化学习(reinforcement learning, RL)**：另一种训练方式，不同于监督学习需要"标准答案"，RL只需要一个"奖励信号(reward)"——一个数值，代表"这次做得好不好"，不需要告诉模型具体该怎么做才对。模型（这里通常称为"策略(policy)"）通过不断尝试、根据奖励信号调整自己的行为，学会在什么情况下做什么动作能拿到更高的奖励。这份笔记后面会反复用到"策略"、"奖励"、"策略梯度"等RL领域的概念，会在具体涉及的地方就地展开。

**预训练(pretraining)与微调(fine-tuning)**：训练大模型通常分两步。第一步"预训练"是在海量、不需要人工标注的数据上做初步训练（第2节会详细展开这具体是怎么做到不需要标注的），让模型具备通用的语言/图像理解能力；第二步"微调"是在预训练模型的基础上，用少量、针对特定任务的数据继续训练，让模型更适应具体的应用场景。这份笔记里讨论的绝大多数方法，都是在这个"预训练之后"的阶段展开的。

---

## 0. 统一视角：把所有方法放进同一个框架里看

几乎所有"自迭代"方法都可以用同一组问题来解构：

1. **生成器（Generator）是谁？** 是当前模型本身，还是模型的历史版本，还是一个专门的任务/数据生成组件？
2. **候选是什么？** 训练数据、推理路径、指令、代码、策略、任务本身——生成的对象在不同方法里差别很大。
3. **评估者/验证者（Evaluator）是谁？** 规则/答案匹配、代码执行器、另一个训练出来的奖励模型、模型自身的自评、人类。
4. **评估信号有多"硬"？** 这是贯穿整个领域的核心变量。可以排一个从强到弱的序列：
   - 形式化验证器（数学证明、代码执行结果、物理引擎里的客观结果）—— 最强，几乎不会被"骗"
   - 过程奖励模型/裁判模型（训练出来的、专门评分的模型）—— 中等，有被"reward hacking"（奖励作弊/钻分数漏洞：被优化的对象学会了投机取巧地拿高分，而不是真的把事情做好，比如发现打分模型偏爱长回答，就故意写得又长又啰嗦来骗分，而不是提升真实质量）的风险
   - 模型自评（同一个模型既生成又打分）—— 最弱，容易自我强化错误的偏好
5. **闭环的更新对象是什么？** 只是当次推理输出（test-time，如Self-Refine），还是模型参数（training-time，如STaR/SPIN/Self-Rewarding），还是连生成器/评估器本身的策略也在被更新（如BigBang）？

《Recursive Self-Improvement in AI》(2026) 这篇综述基本就是用这套坐标系（改进对象 × 闭环封闭程度）去归类1250篇论文的。记住这个框架，几乎可以推演出为什么每个具体方法要这样设计。

---

## 1. AlphaGo Zero（理论原型）

### 关键机制细节
- 网络结构：一个卷积网络，双头输出 (p, v) = f_θ(s)，p是落子概率分布，v是当前局面的胜率估计
- 自我对弈使用**MCTS（Monte Carlo Tree Search，蒙特卡洛树搜索）**：一种通过大量随机模拟对局来估计"当前局面下每种走法未来胜率"的搜索算法。AlphaGo Zero里，网络给搜索提供"先验概率"(往哪个方向搜更有希望)和"局面评估"(不用搜到底就能估出大概胜率)，大幅减少了需要搜索的分支数量；搜索完成后，会得到一个比网络原始输出p更精确的落子分布 π（因为它综合了大量模拟对局的结果，不只是网络的一次前向预测）
- 训练目标：让网络的p逼近搜索后的π（用交叉熵/KL），让v逼近实际对局的最终胜负z（用MSE）
  - 损失：L = (z - v)² - π·log(p) + c||θ||²
- 关键循环：网络指导搜索 → 搜索生成更好的走子和训练标签 → 用这些标签训练网络 → 更好的网络指导更好的搜索……这是一个自增强的正反馈循环
- 这个循环之所以稳定收敛，是因为围棋有**离散、确定、零和、信息完全**的博弈结构，胜负判定是规则给定的、免费的、绝对可信的验证器——这是AlphaGo Zero能做到"零人类数据"的根本前提，也是它最大的局限：这套条件在自然语言/开放世界任务里几乎不存在。

### 补充说明
- **AlphaGo（第一代）和AlphaGo Zero的区别**：第一代先用人类棋谱做监督预训练，再做self-play微调；Zero完全从随机初始化开始，只靠self-play，最终反而更强，说明人类棋谱数据在这个任务里甚至可能是一种"天花板"/噪声源。
- **这套方法为什么在国际象棋(AlphaZero)、将棋上也能复现**：因为这些游戏同样具备"确定+零和+完全信息+规则自带验证器"的结构，AlphaZero本质是把AlphaGo Zero的框架去掉围棋专属的先验知识，做成通用博弈算法。

---

## 2. 自生成数据方向

### STaR：几个关键前提

**"预训练"是怎么让模型有初始语言能力的**：这一步跟自迭代本身没直接关系，但既然后面反复提到"预训练模型"这个起点，有必要说清楚它是怎么来的，这跟常见的（有标注数据的）监督学习不是一回事。预训练用的是**自监督学习(self-supervised learning)**，不需要人工标注："标签"直接从原始文本里自动构造出来——最常见的任务是"下一词预测(next-token prediction)"：把互联网上收集来的海量文本（网页、书籍、代码等，规模是万亿级token）切成一段一段，每一段的前面部分当输入，紧跟着的下一个词当"标签"，训练模型预测"给定前面这些字，下一个字最可能是什么"。因为文本本身天然自带这个"标签"（就是文本里下一个字符/词本来是什么），完全不需要人工标注，可以无限规模地喂数据。

模型在千亿级参数、万亿级token上把这个"预测下一词"的任务做到足够好之后，会**副产品式地**习得语法、常识、事实知识，甚至初步的推理模式——因为要精确预测下一个词，实际上需要在某种程度上"理解"上下文的语义和逻辑（比如要续写"3加5等于"后面的词，模型如果统计规律学得好，大概率会倾向续写"8"）。这个阶段结束后得到的模型，就是STaR、CAI、Self-Rewarding等所有后续方法共同的"起点"——一个已经具备语言能力和某种程度推理能力、但还不会"以对话助手的方式规规矩矩回答问题"的基础模型，需要再经过SFT（第4节CAI部分会讲）才会说话像个助手。

**这跟few-shot提示的关系**：正因为预训练学到的是"续写模式"的能力，所以只要在输入里放几个"问题-推理-答案"格式的示例，模型就能识别出这个格式并模仿续写——这不是训练，纯粹是预训练阶段学到的"模式续写"能力在推理时被直接调用。

**初始模型不是随机的**：STaR用的正是这样一个已经**预训练过**的LLM，已经具备基础的语言能力和一定推理能力，只是还不擅长"稳定地写出高质量推理链"这个具体技能。STaR不是要从零教会模型推理，而是要**放大**它已有的、通过精心设计的提示能激发出来的推理能力，让它稳定地内化成默认行为（不用每次都靠提示技巧激发）。这一点很关键：如果初始模型太弱（比如小到完全不具备基础推理能力），下面说的"few-shot提示"也激发不出可用的推理链，STaR这个自举循环就无法启动——论文自己也指出这依赖于模型已经有一定规模/能力。

**什么是few-shot（少样本提示）**：这不是训练，是**推理时**的技巧，不更新模型参数。做法是在给模型的输入(prompt)里，先放几个（STaR论文里是个位数，比如4-8个）人工写好的"问题+推理链+答案"示例，再放真正要问的新问题，模型会模仿示例的格式和思路续写出答案。举例：
```
Q: 一个小狗可以用什么装着带走？选项：(a)游泳池 (b)篮子 (c)狗展 (d)后院 (e)自己家
A: 答案必须是能用来装小狗的东西。篮子是设计用来装东西的。所以答案是篮子(b)。
[... 再放几个类似的示例 ...]
Q: (真正要问的新问题)
A: (模型接下来续写的内容)
```
之所以有效：LLM在海量文本上预训练后，具备"根据上下文模式续写"的能力，示例相当于临时"教"它这道题该用什么格式、什么风格作答。**这几条示例是人工写好、固定不变的种子，不是模型自己生成的**——STaR"自迭代"的地方在于后面对**新问题**生成推理链和答案这一步，不是这几条示例本身。

**推理链(rationale)是什么样的数据形式**：就是纯自然语言文字，是模型写的一段"思考过程"（跟常见的chain-of-thought提示生成的文本是同一种东西），最后跟着一个最终答案，不是什么特殊的结构化数据。

### STaR 算法细节

每轮迭代对训练集里的每个问题 (x_i, y_i)（y_i是数据集自带的标准答案）：
1. 用当前模型 + few-shot示例生成推理链 r_i 和预测答案 ŷ_i
2. 如果 ŷ_i = y_i（答案正确）：保留 (x_i, r_i, y_i) 加入本轮微调数据
3. 如果 ŷ_i ≠ y_i：触发"rationalization"（倒推/合理化）——把正确答案 y_i 作为提示喂给模型（"已知答案是y_i，请给出推理过程"），让模型反向编出一条能导出y_i的推理链 r_i'，同样加入微调数据
4. 汇总本轮所有数据，微调模型（每轮都从最初的预训练模型重新微调，而不是在上一轮微调后的模型基础上继续训练，以避免多轮误差累积）
5. 重复，直到效果不再提升

关键洞察：如果没有rationalization这一步，循环会在"模型解不出来的题"上停滞——模型永远学不会那些它一开始就不会做的题，因为没有直接的训练信号指向它们。rationalization相当于给了一条"抄近路"的训练信号。

**值得强调的一点**：自迭代不要求生成过程"诚实"——rationalization生成的推理链未必是模型解题时的真实内部计算过程，是一种"事后合理化"，但作为训练信号它依然有效。这跟"事后解释 vs 真实计算路径"的张力是同一个问题的另一种表现形式。

**"只靠这些训练数据，能训练出足够聪明的模型吗？"**——需要澄清一个常见误解：STaR不是要把一个弱模型训练成远超它底子的强模型，而是要把模型"已经会、但要靠精心设计的few-shot才能稳定发挥"的推理能力，通过微调变成"不需要提示也能默认发挥"的稳定能力，同时把这种能力**泛化到没见过的新问题**上。它的能力天花板受限于初始模型本身的推理潜力——论文的比较对象也很诚实：STaR训练出来的模型效果"相当于"一个大30倍的模型直接微调预测答案的效果，不是说STaR能让小模型超越大模型的能力上限，而是说这种"自己生成推理过程再学习"的方式，比"直接学答案"这种更粗糙的监督方式，能更充分地挖掘模型自身潜力。

### Self-Instruct 流程细节

**先纠正一个关键点**：175个人工写的种子任务，**不是只有任务要求**，而是"1条指令(instruction) + 1个实例(instance，即一对具体的输入输出样例)"，也就是每个种子任务本身就是完整的三元组（指令、输入、输出）。后面要让模型生成新任务的输入输出样例，靠的正是这些种子任务本身自带的样例格式做示范。

**"任务(task)"具体指代什么，是不是等于"输入(input)"**：这里"task"是一个比"输入"更宽的概念，等于"指令(instruction)"，即一件要模型去做的事情，例如"把这段话翻译成法语"或"判断这段文本情感是正面还是负面"。"输入(input)"是这个任务具体作用的对象（比如那段要翻译的话），"输出(output)"是完成任务后的结果。有些任务的instruction本身已经完整到不需要额外input（比如"讲一个关于爱情的笑话"），这时input留空；有些任务的instruction是通用模板，需要具体input才能确定要做什么（比如"判断以下文本的情感"，必须配一段具体文本才能执行）。

**流程**：
1. 种子任务池（175条，每条=指令+输入输出实例）
2. **第一步 指令生成**：从池中随机采样几条已有指令，作为few-shot示例，prompt模型生成一条**全新的指令**（这一步只生成任务要求本身，还没有具体输入输出）
3. **第二步 判断任务类型**：判断这条新指令对应的任务，更适合先想好输出再倒推输入(output-first，比如分类任务，先定好类别标签再编一段符合该标签的文本)，还是先给输入再生成输出(input-first，比如开放生成任务)
4. **第三步 实例生成**：根据判断结果，让模型继续生成这条新指令对应的具体输入输出样例
5. **第四步 过滤**：用ROUGE文本相似度等规则，剔除跟已有指令过于相似的、格式不对的
6. 通过过滤的"指令+样例"加回任务池，回到第2步，让池子越滚越大
7. 最终积累约5.2万条指令，配约8.2万条输入输出实例，用来微调GPT-3本身

**"模型需要同时兼顾生成任务和解决任务，这合理吗"**——是的，这正是Self-Instruct的设计：模型需要同时具备"生成新指令(出题)"和"针对指令生成输入输出样例(解题/造数据)"这两种能力，这两个角色都是**同一个模型**在不同prompt下扮演的，没有分成两个独立模型。这是"自迭代"里一个反复出现的模式：不新增模块，靠prompt切换角色。

跟STaR的区别：STaR是"同一批固定问题反复精炼推理链"（扩展解题深度，问题集合不变），Self-Instruct是"不断生成全新的问题/指令本身"（扩展任务广度，问题集合本身在增长）。

---

## 3. Self-Refine（自我反馈精炼）

### 三个角色到底在生成什么
**"生成修改后的版本"指的是任务的输出内容本身**（比如一段修改后的代码、一段修改后的文章、一个修改后的答案），**不是修改prompt**。三个角色更准确的描述：
- **Generator**：输入是原始任务描述，输出是"初始答案"（一段文本/代码等，具体形式取决于任务类型）
- **Feedback**：输入是"原始任务描述 + 当前这版答案"，输出是一段**关于这版答案哪里有问题**的自然语言反馈（比如"这段代码在处理空输入时会报错，且第3行逻辑有误"），本身不修改任何内容，只诊断问题
- **Refine**：输入是"原始任务描述 + 当前这版答案 + 刚才生成的反馈"，输出是一版**新的、改进后的答案**（同样是任务输出本身，格式和Generator的输出一致，只是内容更好）
- 下一轮时，"当前这版答案"就更新成上一轮Refine刚生成的新答案，反馈和精修都基于这个最新版本进行——**只带当前最新的答案+反馈**，不会把好几轮之前的所有历史答案和反馈都堆进prompt（那样确实太长）

### 为什么不是无限循环
- 实践中设置固定轮数（如4轮）或让模型自己判断"不需要再改了"然后停止
- 存在收益递减甚至反效果的情况：如果任务本身模型能力有限（比如复杂数学题），自我反馈可能在错误的方向上"精修"，反而固化错误——这是这条路线最大的软肋，也是它不做参数更新、只做test-time迭代的原因之一（不会把这种"自我强化的错误"写进权重里，损失可控）

### 和agent架构的关系

这其实就是agent领域里常说的"反思(reflection)"模式最朴素的版本——很多agent框架里的self-critique/self-correction循环(比如让agent执行完一步后自己检查结果、发现问题再重试)，理论源头基本就是Self-Refine这类工作。反过来说，这类自我修正机制暴露出的局限也是通用的：**评判者和生成者是同一个模型时，容易出现"自己骗自己"**——模型可能会收敛到一个"读起来更像是改进了，但实际质量没有真正提升"的输出，因为它没有独立于自己判断力之外的标准。这也是为什么后面第4-5节要引入"AI评委"这个角色的独立性(哪怕仍然是AI在评，也尽量让评判标准更结构化、更少受生成时那一次判断的直接影响)——本质上是在弱化"生成者和评判者共享同一套盲点"这个风险。

---

## 4. Constitutional AI（自对齐，RLAIF的奠基工作）

CAI分两个阶段，**这两个阶段依次执行一次，不是像Self-Rewarding那样反复循环多轮**（论文里明确是SL-CAI一次、RL-CAI一次，属于"两步走"而非"迭代N轮"）。

在讲CAI具体做了什么之前，需要先解释它是在"改造"哪个流程——**传统RLHF（Reinforcement Learning from Human Feedback，基于人类反馈的强化学习）**，这是2022年前后训练ChatGPT类模型的标准做法。这套流程的起点，就是第2节提到的那个"预训练模型"（靠自监督的下一词预测，在海量无标注文本上训练出来、已经具备语言能力和常识的基础模型），在此基础上：

1. **SFT（Supervised Fine-Tuning，监督微调）**：收集一批人工写的"问题+高质量回答"，用标准的监督学习（下一词预测的交叉熵损失，跟预训练用的损失函数是同一种，只是数据换成了人工写的高质量问答对，且规模小得多）微调模型，让它学会"以助手语气回答问题"这种格式和风格（预训练模型本身只会"续写"，不会主动"以助手身份对话"，这一步就是补上这个转变）
2. **收集人类偏好数据**：给同一个问题，让模型生成两个不同回答，找人类标注员标注"哪个回答更好"（是两两比较，不是打绝对分数——人类对"绝对好坏"打分不稳定，但"这两个哪个更好"判断相对稳定）
3. **训练偏好模型/奖励模型（Preference Model / Reward Model, PM/RM）**：这是一个**单独训练的神经网络**，输入"问题+一个回答"，输出一个标量分数，训练目标是让它给人类标注为"更好"的回答打更高分。它不生成文本，只做打分这一件事，判断标准完全来自训练它时用的那批人类偏好数据
4. **PPO（Proximal Policy Optimization，近端策略优化）强化学习微调**：把第3步训练好的PM当作"环境反馈的奖励"，用PPO调整主模型参数，让它生成的回答能从PM那里拿到更高分。具体是怎么"调整参数"的，展开说：

   **先把"生成一段回答"看成一个RL问题**：模型逐个token地生成文本，可以把这个过程看作一个连续决策过程——每一步的"状态(state)"是"提示+已经生成的token"，"动作(action)"是"选哪个token作为下一个输出"，模型本身就是"策略(policy)"，即给定状态，输出选择每个可能token的概率分布。一段完整回答生成完毕后（一个"episode"结束），把整段回答喂给PM打一个分数，这个分数就是这个episode的"奖励(reward)"（通常还会减去一个KL惩罚项，衡量当前模型和没做RL之前的SFT模型差多远，防止模型为了骗高分而生成越来越怪异、偏离正常语言分布的内容——这是RLHF里防止"reward hacking"最基本的手段）。

   **策略梯度的直觉**：RL优化的基本思路叫"策略梯度(policy gradient)"——如果某个动作序列（某种生成方式）最终换来了比"预期"更高的奖励，就调整参数，提高未来选择这类动作的概率；如果换来的奖励比预期低，就降低这类动作的概率。这里的"预期"由一个额外训练的**价值网络(value network/critic)**估计（输入当前状态，预测"按当前策略继续生成下去，未来大概能拿多少奖励"），实际奖励减去这个预期值，得到的差叫"优势(advantage)"——优势为正就提高该动作概率，为负就降低，这样比直接用原始奖励更新更稳定（减少方差）。

   **PPO的"Proximal(近端)"具体指什么**：直接按策略梯度算出的方向更新参数，步子如果太大容易一下子把模型带偏、训练不稳定甚至崩掉。PPO的核心技巧是限制每一步参数更新的幅度：计算"新策略在这个动作上的概率 / 旧策略（更新前）在这个动作上的概率"这个比值r，如果r超出一个小范围（比如[0.8, 1.2]，具体范围是一个超参数），就把它"夹紧(clip)"在这个范围内，不让单次更新在某个方向上冲得太猛。这就是"近端"的含义——每次更新都被限制在离旧策略"不太远"的一个邻域内，从而避免训练发散。

   **为什么工程上复杂**：完整跑一次PPO需要同时维护和运行四个模型：(1) 正在训练的策略模型（主模型本身）；(2) 一个冻结不变的参考模型（SFT后、还没做RL的版本，用来算KL惩罚）；(3) 训练好的奖励模型PM（打分用，不参与训练）；(4) 价值网络（估计"预期奖励"，需要单独训练）。四个模型同时占显存、同时参与前向计算，这也是后面DPO（第5节会讲）想要绕开PPO、简化流程的直接动机。

这个流程的**成本瓶颈**全在第2步：需要海量人类标注员反复标注偏好数据。CAI要解决的正是这一步。

### 阶段1：SL-CAI（监督阶段）——把"人类标注有害样本"换成"AI自我批评"

**红队提示从哪来、"红队"为什么这么叫**："红队(red team)"是安全测试领域借用的军事术语，指专门扮演"攻击者"角色去寻找系统漏洞的团队。这里的"红队提示(red-team prompt)"就是专门设计出来、试图诱导模型说出有害内容的问题，用来测试模型的安全边界。这批提示不是模型自己划分的，是研究者提前准备好的一批问题集合：一部分是之前工作里众包标注员手写的挑衅性问题（约4.2万条，人类扮演攻击者，专门跟模型对话试图"套话"），另一部分是用few-shot方式（见第2节的解释）让模型自己仿写扩大规模的（约14万条），合计约18.3万条，跟其他普通的"帮助性"问题分开处理。

**具体步骤**（每一步的输入都只带当前这一轮，不叠加长历史）：
1. 用一个已经做过**纯helpfulness RLHF**（只在"有没有帮助"这个维度上训练过，还没针对有害性做过滤）的模型，对红队提示生成初始回答（这一步的输出往往包含不安全内容，这是预期中的）
2. **批评(critique)**：输入="红队提示 + 刚生成的回答 + 随机抽取的1条宪法原则"，prompt模型指出这个回答违反了这条原则的哪些地方
3. **修订(revision)**：输入="红队提示 + 上一版回答 + 刚生成的批评"，prompt模型给出一版修改后的回答（只带当前这一轮的回答和批评）
4. 可以重复步骤2-3多轮（论文里对每条红队提示采样4轮批评-修订，每轮换一条随机原则，逐步把回答打磨得更安全）
5. 汇总所有"红队提示 → 最终修订后的回答"样本，加上普通的helpfulness样本，一起用标准的SFT训练模型——把"修订后的回答"当作新的训练标签，教模型直接学会一步生成这种更安全的回答风格。得到的模型叫SL-CAI

### 阶段2：RL-CAI（强化学习阶段，即RLAIF：Reinforcement Learning from AI Feedback）

这一步的目的：SL-CAI只是"模仿了修订后的回答风格"，效果有上限。RL阶段的目标是用强化学习进一步压榨出比单纯模仿更好的安全性，做法是把上面传统RLHF流程第2步"人类标注偏好"替换成AI标注：

1. 用SL-CAI模型对同一个提示采样两个不同回答
2. 对harmless prompt：让一个独立的AI（用宪法prompt模板，随机抽一条原则问"哪个回答更好地遵守了这条原则"）判断两个回答哪个更好，生成AI偏好标签；对helpfulness prompt，仍然用之前收集的人类偏好标签（这部分没有换成AI）
3. 汇总AI标注的harmlessness偏好 + 人类标注的helpfulness偏好，训练一个偏好模型PM（做法和传统RLHF训练PM完全一样，只是部分标签来源换了）
4. 用这个PM作为奖励信号，跑PPO微调SL-CAI模型，得到最终的RL-CAI模型

**宪法prompt怎么设计最好**：论文准备了16条不同角度、有一定重叠的原则（比如"选择更不可能被视为有害的回答"、"选择更倾向不鼓励非法行为的回答"），每次批评/判断时**随机抽一条**，而不是把16条都堆给模型一次判断。好处：(1) 避免prompt过长；(2) 随机抽取相当于多角度审视，能提高鲁棒性和多样性，不会让模型只学到某一条原则的表面特征。原则写得越具体越不模糊，AI判断的准确率越高——论文验证过，这种二选一判断的准确率已经能接近人类标注员水平。

**核心直觉**：人类的监督劳动，从"标注海量具体样本(RLHF)"，上移到"写16条抽象原则(CAI)"——判断权下放给AI，但判断的"标准"仍然是人写的。

---

## 5. Self-Rewarding Language Models（自奖励，比CAI更进一步）

### 起点的"两个能力"具体是什么、怎么来的
论文假设从一个"预训练模型 + 少量人类标注种子数据"出发，目标是让模型同时具备：
1. **指令跟随能力(Instruction Following)**：给一个用户请求，能生成有帮助、无害的高质量回答——这就是上一节讲的标准SFT要练出来的能力，论文实际做法是用Llama 2 70B在一个公开人类对话数据集(OpenAssistant)上做SFT，得到种子模型
2. **自我指令创造能力(Self-Instruction Creation)**：既能自己生成新的instruction-following训练样例（新prompt+新回答，类似第2节Self-Instruct的做法），也能给这些样例的质量打分

**这个"打分"能力怎么来的，需要单独训练一个打分网络吗？**——不需要。这里用到**LLM-as-a-Judge（大模型当裁判）**这个手法：把"评价一个回答好不好"本身也变成一个prompt任务，给LLM一个模板，例如"以下是一个问题和一个回答，请从有帮助性、准确性、清晰度等维度打分(0-5分)，并给出理由"，模型输出的就是一个评分。这本质上是把同一个指令跟随能力强的LLM，用不同的prompt引导去执行"打分"这个任务，不需要重新训练一个专门的打分网络——所以准确地说，"两个能力"不是分别独立训练出来的两个模块，而是**一个通用LLM通过不同prompt模板被引导执行不同任务**。

### 每轮迭代的完整流程
1. **生成新prompt**：当前模型M_t自己生成一批新的instruction-following测试问题
2. **生成候选回答**：M_t对每个新prompt生成多个（论文里是4个）候选回答
3. **自我打分**：M_t用LLM-as-a-Judge的prompt模板，给每个候选回答打分(0-5分，基于详细的评分细则)
4. **构造偏好对**：取分数最高的作为chosen(y_w)，分数最低的作为rejected(y_l)，构成偏好对(x, y_w, y_l)
5. **DPO训练**：用这批自产的偏好对训练模型，更新出下一版模型M_{t+1}

**什么是DPO（Direct Preference Optimization，直接偏好优化）**：这是2023年提出的方法，目的是跳过"训练单独的偏好模型 + 跑PPO"这两步（上一节CAI用的就是这套），直接用偏好数据优化生成模型本身。输入数据格式是三元组(x, y_w, y_l)——一个问题，一个更好的回答，一个更差的回答。

**先解释一下记号 pθ(y|x) 是什么意思**：这是概率论里标准的"条件概率"写法，读作"给定x的条件下，y的概率"。放到LLM语境里：
- x 是输入的问题/提示(prompt)
- y 是一整段回答（一个token序列，比如y = [y_1, y_2, ..., y_m]）
- θ 是模型参数
- pθ(y|x) 就是"参数为θ的这个模型，在看到输入x之后，生成出恰好是y这段完整回答的概率"

这个概率具体怎么算：语言模型是**自回归(autoregressive)**的，一个词一个词往后生成，每生成一个词时用softmax输出"下一个词是词表里每个候选词"的概率分布。所以一整段回答y的概率，等于把生成过程中每一步"选中当前这个词"的概率**连乘**起来：
```
pθ(y|x) = pθ(y_1|x) × pθ(y_2|x,y_1) × pθ(y_3|x,y_1,y_2) × ... × pθ(y_m|x,y_1,...,y_{m-1})
```
即"第1个词在给定x下的概率" × "第2个词在给定x和第1个词下的概率" × ……一直乘到最后一个词。这个连乘积通常是一个极小的数（因为在乘很多个小于1的数），所以实践中几乎总是取**log**，把连乘变成连加：
```
log pθ(y|x) = log pθ(y_1|x) + log pθ(y_2|x,y_1) + ... + log pθ(y_m|x,y_1,...,y_{m-1})
```
这就是为什么DPO/SPIN的公式里都写的是**log pθ(y|x)**，而不是直接写pθ(y|x)本身——数值上更稳定、计算上是连加而不是连乘，是训练LLM时的标准做法（标准的交叉熵损失本质上就是这个量的负数：交叉熵损失是−log pθ(y|x)，只是DPO/SPIN把两个这样的"负log似然"做了一个相减对比，不是直接用它去做最基础的监督学习）。

理解了这一点，"pθ(y|x)相对于p_ref(y|x)高多少"（即log pθ(y|x) − log p_ref(y|x)）就是在说："当前训练中的模型，比参考模型，更愿意生成y这段回答多少"——这就是DPO/SPIN损失函数里反复出现的核心量。

回到DPO本身：核心思路是，数学上可以证明，"训练PM+跑PPO"最终收敛到的最优策略，等价于直接对模型本身做下面这样一个对比式损失函数的优化（下面用纯文本写出公式的结构，不依赖公式渲染器；VS Code默认预览不渲染LaTeX，需要装Markdown+Math一类插件才能看到公式排版，纯文本版在哪都能直接读）：

```
先定义"相对对数似然"（模型比参考模型更喜欢这个回答多少）：
  好回答的相对分数 = β × [ log pθ(好回答|问题) − log p_ref(好回答|问题) ]
  差回答的相对分数 = β × [ log pθ(差回答|问题) − log p_ref(差回答|问题) ]

DPO损失 = − log( sigmoid( 好回答的相对分数 − 差回答的相对分数 ) )
```
其中 pθ 是正在训练的模型，p_ref 是一个固定不变的参考模型（通常是SFT后、还没做偏好优化的那个版本，训练过程中冻结不动，只用来算这个"相对"分数的基准），β是一个超参数（控制这个偏好信号有多强），sigmoid把"好回答分数-差回答分数"这个差值压缩到0~1之间当作一个概率/损失强度。

直觉理解（不用记公式）：这个损失函数在做的事情就是——**拉高**模型对"好回答"相对于参考模型的相对概率，同时**压低**模型对"差回答"相对于参考模型的相对概率，"好回答分数"和"差回答分数"差得越大（好的比差的领先越多），损失就越小。好处是只需要一个正在训练的模型（加一个固定不变的参考模型副本用来算比值），不需要单独训练PM，也不需要跑PPO那一整套复杂机制（不需要价值网络，不需要在线采样生成再打分，直接对一批预先准备好的偏好数据做类似监督学习的梯度下降）。**记住这个结构**：拉高好例子的相对似然、压低差例子的相对似然，都是相对参考基准而言——第6节SPIN会直接用到这个结构。

6. 用M_{t+1}重复步骤1-5（下一轮的生成、打分都用新模型做）

### 什么是AlpacaEval胜率，是怎么算的
论文报告"AlpacaEval胜率提升"。AlpacaEval是一个自动化评测基准：给定一批标准测试问题，让待评测模型和一个固定的参照模型（比如GPT-4）分别生成回答，然后用一个强模型（通常也是GPT-4）当自动裁判，对每一对回答做二选一比较"哪个更好"，统计待评测模型的回答被裁判选中的比例，这个比例就是"胜率(win rate)"——50%代表和参照模型打平，高于50%代表更好。本质上是把"人类标注偏好"换成了"用GPT-4自动标注"，也是LLM-as-a-Judge的一种应用，只是这里用于**评测**而非训练。论文里随着迭代轮数增加(M1→M2→M3)，这个胜率持续提升；同时还专门测了"模型自己打的分，跟人类实际评分的一致程度"，发现这个一致程度也在提升——这才是"评委能力和选手能力同步提升"这个结论的实际证据来源。

### 局限
只跑了3轮，没有验证长期是否会饱和或崩塌；"评委"和"选手"是同一个模型，长期看存在系统性偏差自我强化的风险。

---

## 6. SPIN（自对弈微调）—— 澄清一个常见误解

**一个常见的误解需要先澄清**：SPIN**没有**像GAN那样存在两个独立的网络（一个生成器网络+一个判别器网络互相对抗训练）。整个SPIN只有**一个正在训练的模型**，"判别"这个说法只是类比、帮助直觉理解，实际实现上**不存在一个显式的、可以被训练"变差"来逃避识别的判别器**——所以"判别能力变弱就能让新模型更容易蒙混过关"这种GAN式退化模式在SPIN里根本不会发生，因为没有独立判别器可以被"打败"。

### 真正发生的事情（回顾第5节的DPO公式会容易理解很多）
SPIN在每一轮t，把"上一轮训练好的模型参数θ_t"复制一份，冻结起来，叫做"对手玩家(opponent player)"，只是**当作一个静态的参照基准**去计算相对概率，不参与被训练、也不会"变差"。真正被训练更新的是"主玩家"，也就是新一轮的参数θ_{t+1}。

具体损失函数（和第5节DPO几乎同一个结构，只是"谁是好例子、谁是差例子"的定义方式不同，同样用纯文本写出来）：

```
好例子的相对分数 = λ × [ log pθ(y|x)  − log p_θt(y|x)  ]     ← y 是人类SFT数据里的标准答案
差例子的相对分数 = λ × [ log pθ(y'|x) − log p_θt(y'|x) ]     ← y' 是上一轮模型θt自己生成的回答

SPIN损失 = − log( sigmoid( 好例子的相对分数 − 差例子的相对分数 ) )
```
跟DPO对比着看：DPO里的y_w/y_l（好例子/差例子）对应SPIN里的y/y'；DPO里固定不变的参考模型p_ref，对应SPIN里的θ_t。区别在于θ_t在SPIN里身兼两个角色——既是"生成负样本y'的模型"，也是公式里作为分母的"参照基准模型"（这两个角色是同一个θ_t）；而且这个参照基准**每一轮都换成上一轮训练好的新模型**，不像DPO里的p_ref通常固定不变、从头到尾都是训练最开始的那个SFT模型。

**"隐式地，通过loss设计"到底什么意思**：更准确的说法是——SPIN没有一个专门的、参数独立的判别器网络，"区分正负样本"这件事完全体现在**损失函数的数学形式**里(上面这个log-sigmoid对比式)，而不是体现在一个真实存在、要被单独训练的神经网络模块里。可以理解成：DPO/SPIN这类方法，是把"训练一个判别器/奖励模型+用它做RL"这两步，直接压缩进了一个损失函数里，用代数变换证明了这样做在数学上等价，所以不需要真的实现出那个判别器。

**训练具体怎么做**（对照上面公式，一步步说清楚"模型怎么训练"）：
1. 初始化：θ_0 = 某个已经SFT过的模型
2. 对t = 0, 1, 2, ...（每一轮）：
   a. 用当前模型θ_t，对SFT数据集里每个问题x_i，采样生成一个回答 y_i' ~ p_{θt}(·|x_i)（这就是"对手玩家"要用到的负样本）
   b. 用上面的损失函数，对参数θ做梯度下降更新，正样本用SFT数据集自带的标准答案y_i，负样本用刚生成的y_i'，参照基准都用θ_t（保持不变，只在这一轮内当分母），更新得到 θ_{t+1}
   c. 下一轮(t+1轮)时，θ_{t+1}变成新的"当前模型"去生成新的负样本，θ_t这个旧版本"退休"，不再参与
3. 重复，直到收敛或达到设定轮数

**第一轮开始时，pθ和p_θt明明是同一个模型，这时候损失/梯度是不是就没意义了？**——这是个很自然的问题，答案是：数值上重合，但梯度不为零，训练照样能推进。拆开看两件事：

- **损失的初始值**：第0轮刚开始做梯度下降之前，θ和θ_0确实是完全一样的参数，此时"好例子的相对分数"= λ×[log pθ(y|x) − log p_θ0(y|x)] = λ×[相同的数 − 相同的数] = 0，"差例子的相对分数"同理也是0，两者相减还是0，损失 = −log(sigmoid(0)) = −log(0.5) ≈ 0.693，是一个正常的、非零但也不夸张的初始损失值——不是"没意义"，就是一个正常的起点。

- **关键在于梯度，不在于起点数值相不相等**：θ_0这个参照基准在参数更新的过程中是**冻结的常数**（不参与反向传播，相当于PyTorch里对它做了detach），损失函数里"作为分母/参照基准的p_θt"这部分不贡献梯度，只有"作为被训练对象的pθ（分子）"这部分才对参数θ求导。所以哪怕θ和θ_0数值上重合，损失函数对θ的梯度依然是一个良定义、非零的方向：梯度指明了"往哪个方向调整θ，能让pθ(y|x)（好例子）变大、pθ(y'|x)（差例子）变小"——这跟"θ和θ_0现在数值相等"完全不矛盾，就像站在山坡上某一点，即使这一点刚好是出发点，坡度（梯度）依然指向一个确定的下坡方向，迈出一步，位置就变了，不再和出发点重合。

  更直白地说：一旦顺着这个梯度方向迈出第一步（更新参数），θ就和θ_0（现在已经"退休"、不再变化）产生了差异，pθ开始比p_θ0更偏向"提高好例子概率、压低差例子概率"，循环由此正式启动。类比AlphaGo Zero：第一局自我对弈时，网络也是在"和自己一模一样的一个副本"下棋，同样不妨碍训练照常进行——因为训练用的是"这一局的结果"这个新信息去更新参数，不是靠两个网络"当前有没有差异"来启动。

### 为什么这样设计能让模型变强、且不会退化
论文的理论保证：只要这个损失函数满足一定条件（单调递减、凸函数——像logistic loss、hinge loss都满足），可以证明这个训练目标的**唯一全局最优点**恰好是"θ生成的分布 = 人类标注数据分布(p_data)"的时刻。也就是说，无论初始模型多弱，只要不断按这个目标迭代，理论上模型的输出分布会被推着不断逼近人类高质量数据的分布，逼近到头就是"和p_data一致"，不会无限跑偏——这就是SPIN不会持续退化的数学依据。直觉上：正样本(y)永远是人类给定的高质量答案，不会变，模型每一轮都在被"拉向"这个固定的锚点，只是"跟谁比"（负样本）在逐轮变强，所以每一轮的"区分任务"难度也在逐轮提高，模型被迫学得更精细才能拉开差距。

### 和DPO的三点具体区别
1. DPO是"一次性"方法：给定一批偏好数据，训练一次得到最终模型，没有内在的"迭代"机制；SPIN的自对弈机制天然是迭代的，每轮的对手都换成上一轮的新模型
2. DPO需要**人工标注或外部模型给出的偏好对**(x, y_w, y_l)；SPIN只需要**原始SFT数据**(x, y)，不需要任何额外偏好标注——负样本(y')是模型自己在训练过程中现场生成的
3. SPIN可以在SFT之后、RL微调之前插入使用，作为一个不需要额外标注成本的中间加强步骤

---

## 7. 综述给出的谱系（承上启下）

### A Survey on Self-Evolution of LLMs 的四阶段框架
Experience Acquisition（获取经验：探索/生成新数据或任务）→ Experience Refinement（精炼：过滤/修正/增强原始经验）→ Updating（更新：用精炼后的经验更新模型，可以是参数更新也可以是记忆/工具库更新）→ Evaluation（评估：衡量更新后的效果，决定是否继续下一轮、往哪个方向调整）→ 回到Acquisition

这个框架的价值在于：几乎每个具体方法都能映射进这四步，且可以清楚看出"每个方法在哪一步做了创新"：
- STaR/Self-Instruct 的创新在Acquisition（怎么生成新经验）
- Self-Refine 的创新在Refinement（怎么精炼已有输出）
- CAI/Self-Rewarding 的创新在Evaluation（谁来评估、评估信号从哪来）
- SPIN 的创新在Updating（用什么训练目标把经验转化为参数更新）

### Recursive Self-Improvement 综述（2026）的两轴分类法
- 轴1（改进对象）：deployment behavior（部署时行为，如Self-Refine）/ training policy（训练中的策略，如STaR/SPIN）/ evaluator（评估器本身，如Self-Rewarding）/ research process（研究过程本身，如BigBang、AlphaEvolve、Autonomous Post-Training）
- 轴2（闭环封闭程度）：human-in-the-loop（每轮都需要人类介入或校准）到fully closed（完全自主闭环）
- 核心论断：把"有限自我修正"（bounded self-refinement）——收敛、可评估、已经是工业界常规实践——与"开放式递归自我改进"（open-ended RSI）严格区分。后者仍受制于三大瓶颈：
  1. **接地（grounding）要求**：改进信号必须最终锚定在某种客观现实（物理结果、代码执行、可验证的数学）上，否则容易在"虚假的自我认可"中打转
  2. **模式崩溃（collapse）动态**：自产数据反复训练自己容易导致多样性坍缩、错误自我强化（这是Self-Rewarding等方法长期运行的主要风险点）
  3. **算力约束**：即便机制上可行，闭环运行本身（尤其是涉及大量"rollout"——这个词后面会反复出现，指"让模型/策略从头跑一遍生成或执行过程，拿到一个完整结果"，比如让LLM完整生成一段回答、或让机器人策略完整执行一次任务，都算一次rollout——以及大量采样/验证的方案）需要的计算资源可能远超线性增长
- 评估器信号可靠性排序：formal verifiers（形式化验证器，最强）> process reward models / judges/rubrics（过程奖励模型/裁判/评分规则）> intrinsic self-assessment（模型内在自评，最弱）——这条排序几乎可以解释后面BigBang/Absolute Zero为什么反复强调"可验证性"。

---

## 8. BigBang 细节补充

先把几个会反复出现的名词/角色关系说清楚，再讲机制，避免下面出现"突然蹦出来的专有名词"。

### 先分清楚：谁是"被训练的模型"，谁是"负责生产数据的agent"

这是理解BigBang架构最容易搞混的一点：**BigBang-V1（最终产出、被训练的目标模型）和Generator Agent/Critic Agent，不是同一个东西**。

- **BigBang-V1**：这是最终要交付使用的LLM，起点是Qwen3.6-35B-A3B（一个已经预训练好的基座模型），整套流程结束后，用筛选出来的高质量任务数据对它做SFT（第4节解释过的监督微调），得到BigBang-V1
- **Generator Agent**：论文原文明确说它"operates as a code agent"——即一个**能读写代码、能执行代码、能调试代码的智能体**，它的工作对象不是"直接回答问题"，而是**编写和修改"数据合成代码"本身**（比如生成任务用的prompt模板、任务筛选逻辑、验证脚本等）。也就是说，Generator Agent不是在用嘴回答问题，而是像一个程序员一样，写代码去生产一批又一批的候选任务和候选解答，然后跑这些代码得到结果
- **Critic Agent**：负责审查Generator Agent生产出来的候选任务/解答质量，把评估结果反馈给Generator Agent

论文没有明确公开Generator Agent/Critic Agent具体是由哪个基座LLM驱动的（可能是同一家的某个更强/更贵的模型，专门用来做"数据生产"这个工序，跟最终被训练、被交付的BigBang-V1是分开的两件事）——可以类比成："雇一个更贵的资深工程师(Generator/Critic Agent)，专门给一个正在培养的新人(BigBang-V1)出练习题、判练习题对错"，资深工程师本身不是被培养的对象。

### Generator Agent具体在"生成任务"和"解决任务"之间怎么切换

Generator Agent既负责生成任务又负责解决任务，这两件事怎么在同一个agent身上同时发生——机制上确实是**同一个agent在不同阶段执行不同的子任务，靠不同的指令/代码逻辑区分角色**，不是分裂成两个独立模块。具体展开成三件事：

1. **任务构造(Task Construction)**：Generator Agent写代码/调用工具，构造出一个新的候选题目（论文里覆盖的领域包括Search检索、Coding编程、AI Research、Scientific Research等）
2. **求解(problem solving)**：Generator Agent自己去解这道刚构造出来的题目，产出候选解答
3. **结果校验(result verification)**：Generator Agent对自己的解答做初步自查（比如代码题就跑一下测试用例）

这三件事都被Generator Agent当作"可以被代码化、被优化的策略"来对待——这是它区别于前面几节所有方法的地方：前面几节（STaR/Self-Refine/CAI等）里，"生成"和"评估"通常是**直接用prompt调用LLM做一次推理**；而BigBang里，Generator Agent面对的是"怎么写一段能稳定产出高质量候选任务的代码逻辑"这个更高层的问题——它优化的对象是**生产数据的程序**，而不是某一条数据本身。

### Critic Agent具体怎么评价、又怎么把反馈传回给Generator Agent

论文说Critic在两个层次上检查候选数据：
1. **基础约束检查(basic constraints)**：候选题目/解答里的推理过程和交互记录信息是否完整、工具有没有被正确使用等——这一层只做"及格线"式的合法性检查，不合格直接拒绝
2. **更高层的质量证据(higher-level evidence of quality)**：评估这道题的正确性(correctness)、可验证性(verifiability)、难度(difficulty)、训练价值(training value)等更细致的维度——这一层是"这题到底有没有价值，值不值得拿去训练模型"

反馈是怎么传回Generator Agent的：Critic把这些评估结果（哪里不合格、哪个维度分低）作为**文字化的反馈信息**交给Generator Agent，Generator Agent（作为一个能读写代码的agent）据此**直接修改自己生成候选任务/候选解答用的那段代码/prompt逻辑**，比如论文提到Generator Agent在这个过程中自主发展出了一些没有被显式指定的行为：会去修订自己的生成prompt让任务边界更清晰、会给合成流程加入新的质量检查和候选淘汰机制——这些都是Generator Agent在"编程"层面上做的自我改进，不是在改BigBang-V1的模型参数。

**这就是"Strategy Update"机制的准确含义**：不是更新BigBang-V1的参数，而是Generator Agent（和它背后那段数据合成代码）根据Critic的反馈，不断修改自己"怎么出题、怎么解题、怎么自查"这套代码策略本身。论文把这个过程描述成"一个在程序空间里的持续搜索"——可选的生成策略、验证策略、筛选策略构成了一个很大的"程序空间"，Generator Agent在这个空间里不断试错、调整，找到更好的数据生产方式。

### 什么是"held-out真实任务"，谁来评估，评估结果怎么被用上

**held-out（留出）真实任务**：这是机器学习里的常规概念，不是BigBang专属——指从"真实存在的研究任务"（人类科学家实际会遇到的问题，不是Generator Agent合成出来的）里，专门留出一批**完全不参与训练**的题目，只用来做最终评估，防止"自己出的题、自己判、自己还拿这批题当训练集"这种循环论证。

**谁来评估**：这一步不是Critic Agent，而是**直接看BigBang-V1这个被训练出来的模型，在held-out真实任务上的客观表现**（论文用的评测方式，是标准的benchmark打分——跑模型在这批真实任务上的表现，看对错/分数，跟常见的机器学习模型评估是同一套逻辑，不是靠另一个agent主观打分）。

**评估结果怎么被用上（论文机制原文）**：
1. 定期挑几套有代表性的"数据合成pipeline"（也就是Generator Agent当前那套生成任务的代码策略），各自生产一批数据，训练出几个不同的模型变体
2. 把这几个模型变体拿到held-out真实任务上跑分，把"跑分高低"当作衡量"这套合成pipeline产出的数据到底有没有用"的**实证指标**（论文原话：using their performance as an empirical measure of data utility）
3. 引入一个额外的**meta-critic**（元评判者，是在Critic Agent之上再加一层的比较机制）——把"Critic Agent当时给这批数据打的分"和"这批数据实际训出来的模型在真实任务上的表现"两者做对比，如果Critic给的高分对应的模型表现却不好（说明Critic在系统性地看错），这个差异(discrepancy)就被用来**修正Critic的评判标准**，同时也修正Generator Agent的生成策略

论文没有更进一步展开"具体怎么修正"的算法细节（比如是不是也走一遍类似Strategy Update的"code agent改代码"流程，还是用其他方式调整Critic的判断prompt）——这一层目前只能确认"有一个用真实结果校准评判标准的机制"，具体实现论文没有公开到能让人复现的颗粒度。

**"依据能力短板调整任务合成的领域分布/难度"具体是什么操作**：论文这部分描述也偏宏观（原文用的是"identifying capability gaps and adjusting the difficulty, diversity, and structure of the generated data"），可以理解为——如果held-out评估发现模型在"Coding"这类任务上表现明显弱于"Scientific Research"，下一轮就会让Generator Agent多产出、多训练Coding方向的任务，相当于一个"哪里薄弱就往哪里补课"的机制，但论文没有给出这个"分配比例怎么算"的具体公式。

### 完整的flywheel（论文用的原词，指"飞轮"，即自我强化、越转越快的循环）

真实前沿任务 → 识别当前模型的能力短板 → Generator/Critic交互生产数据 → 得到高质量合成数据 → 用这批数据训练模型 → 在真实任务上验证 → 把验证结果反馈回去，进入下一轮

### 为什么称为"早期的数据层面RSI（递归自我改进）"

论文明确说这是"early, data-level form"的递归自我改进——因为改进的闭环不仅作用在模型能力上（BigBang-V1一轮轮变强），也作用在"生成下一轮改进机会"的机制本身（Generator Agent的代码策略、Critic Agent的评判标准都在被修正），构成了论文强调的"AI推动科学，科学反哺AI"的双向循环雏形。但仍是"data-level"（数据层面）——它没有去自主修改BigBang-V1的模型架构、没有自主决定研究方向，改的始终是"怎么生产训练数据"这一件事，所以还称不上是完全彻底、"无所不包"的RSI，只是这条路上比较早期的一步。

---

## 9. Absolute Zero 细节补充

### 背景：什么是RLVR，Absolute Zero是在它基础上做了什么极端化

**RLVR（Reinforcement Learning with Verifiable Rewards，用可验证奖励做强化学习）**：这是Absolute Zero工作之前就已经存在、被验证有效的一类做法，核心思路是——不需要像CAI/RLHF那样训练一个专门的打分模型(PM/RM)，而是直接用"答案客观对不对"这种规则化的判定结果当作RL的奖励信号。比如一道数学题，答案对就给奖励1，错就给0，不需要任何模型去"评判"这个答案好不好，规则本身就是评判者。这跟第0节说的"评估信号强弱排序"里最强的那一档("形式化验证器")是同一个东西——RLVR就是"用形式化验证器当奖励，然后跑标准RL"的统称做法。

RLVR已经证明这种做法能有效提升模型的推理能力，但**在Absolute Zero之前，RLVR用的题目仍然来自人工整理的问答对数据集**——也就是说，"怎么打分"这一环已经自动化了(不需要人标"好不好")，但"出什么题"这一环还没有：题目本身还是人类事先准备好的。

Absolute Zero要做的极端化，就是把"出题"这一环也交还给模型自己——不仅评判是自动的(继承RLVR)，连"练习册"本身也是模型自己写的，练习册里的每道题也是模型自己批改的。这就是"Zero Data"（不依赖任何人工整理的问答对）这个名字的来源。

### AZR (Absolute Zero Reasoner) 的任务类型
论文里设计了三类可由代码执行器验证的推理任务（对应编程语言的三类基本问题）：
- **演绎（deduction）**：给定程序和输入，预测输出
- **归纳（induction）**：给定一些输入输出对，归纳出程序
- **溯因（abduction）**：给定程序和输出，反推可能的输入

模型在一个统一的RL循环里同时扮演"提出者"（propose，为三类任务生成新的程序/输入/输出组合，目标是最大化对当前模型来说的"学习潜力"）和"求解者"（solve，去解刚才提出的问题）——和BigBang类似，这也是**同一个模型靠不同prompt/角色切换**完成两件事，不是两个独立网络。代码执行器提供两层验证：验证提出的任务本身是否合法可执行、验证求解答案是否正确执行结果一致。

### "学习潜力"具体怎么量化（论文给出的精确公式）

这是一个具体的、可以写清楚的机制，不是含糊的说法：

1. 提出者提出一个任务τ后，让**求解者对这个任务做G次独立的rollout**（第7节提到过rollout指"完整跑一遍生成/求解过程"；这里是蒙特卡洛式的重复试验：同一个任务，用带随机性的采样方式，让求解者反复解G遍，因为每次采样带随机性，G次结果可能不完全一样）
2. 统计这G次里**求解成功的比例**，记作平均成功率 r̄_solve（比如10次里对了6次，r̄_solve = 0.6）
3. 提出者的奖励(reward)定义为：
```
如果 r̄_solve = 0 （G次全错，完全不会做）  → 提出者奖励 = 0
否则                                      → 提出者奖励 = 1 − r̄_solve
```
4. 直觉：如果一个任务**太简单**（求解者次次都对，r̄_solve = 1），提出者奖励 = 1−1 = 0，没有奖励——因为"求解者已经会了，这题练不出新东西"；如果一个任务**完全不可能**（求解者次次都错，r̄_solve = 0），奖励被显式设为0——因为"完全无法从中学到东西，纯粹在浪费算力"；如果任务处于**中等难度**（比如求解者一半对一半错，r̄_solve = 0.5），提出者拿到奖励 = 0.5，是最高的——这正是"既不能太容易也不能太难"这句话背后的精确定义：用求解者当前的实际表现，去衡量一个任务此刻对它有多少"练习价值"，这个概念在自动教学课程设计(unsupervised environment design)和自主学习智能体(autotelic agents)的文献里已有研究，AZR是把它借用到LLM推理任务里
5. 求解者自己的奖励则简单得多：直接是**二元的对错奖励**（答对给1，答错给0），不需要这套"学习潜力"的设计——因为求解者的目标很单纯，就是尽量解对题；"学习潜力"这个更复杂的奖励设计，专门是为了让提出者学会"出恰到好处难度的题"这个更微妙的目标而存在的。

这套机制的巧妙之处在于：不需要人来判断"这道题的难度是否合适"，完全靠求解者自己当前的实际表现（多次采样的成功率）动态决定——随着求解者越来越强，同一道题的r̄_solve会逐渐从"中等难度对应的0.5"往"1"靠近，提出者的奖励信号会自动引导它去构造更难的新题，形成一个难度随能力自动爬升的课程(curriculum)，这也是"自我提出的课程能够开放式地演化"这句话的具体机制来源。

### "Zero Data"的准确含义
不是"不使用任何外部工具"，而是"不使用任何人工标注/人工整理的问题-答案对"。代码执行器是一个确定性的、非学习的外部工具，其"知识"来自编程语言语义本身，而非人类为这个特定任务标注的数据——这是它和STaR/Self-Instruct（虽然也自动生成数据，但通常需要种子任务或依赖预训练模型里已经内化的人类知识）的关键区别。

### 和BigBang的对比：两种不同的"可验证"抓手

BigBang（第8节）和Absolute Zero都反复强调"可验证性"，但两者验证的**抓手**完全不同，这是一个值得对比理解的点：

- **BigBang靠Critic Agent**：本质上还是一个LLM在做判断("这道题合格吗、有没有训练价值")，虽然论文用held-out真实任务持续校准它，但Critic本身仍然是"过程奖励模型/裁判"这一档(第0节评估信号强弱排序里的中间档)，覆盖面很广——理论上任何能被写成"任务"的东西，Critic都能尝试评一评(科研问题、代码、开放式推理都行)，但可靠性要靠额外的校准机制去维持
- **Absolute Zero靠代码执行器**：是一个确定性工具，不是LLM，不需要额外训练或校准，判断结果100%可复现(同样的代码同样的输入，结果一定一样)，属于"形式化验证器"这一档(第0节排序里最强的一档)——但代价是覆盖面窄得多，只能覆盖"答案能被代码执行验证"的任务类型(数学、编程这类)，写文章好不好、一个科研想法是否新颖，代码执行器根本判断不了

这个对比正好呼应第0节提出的核心变量——**评估信号有多硬，跟这个信号能覆盖多广的任务类型，往往是一对需要权衡的东西**：越硬的信号(代码执行器)覆盖面越窄，越广的信号(LLM当裁判)越需要额外机制去防止它被"骗"。

---

## 10. AlphaEvolve 细节补充

先把系统的四个组成部分说清楚（论文Figure 1/2给出的架构），再讲"种群"和"进化算法"具体是什么、"评估器"具体由什么构成，最后讲三个真实落地案例的技术细节，避免停留在"进化算法优化了xx"这种空泛描述。

### 四个组件，以及它们怎么串成一个循环

先说"父程序"和"灵感程序"是什么，这是理解Prompt sampler具体做什么之前必须先弄清楚的两个对象：

- **父程序(parent program)**：这一轮要拿来做修改的"基础版本"——从**Program database**（一个存放所有已生成、已评估过的候选程序及其分数的数据库，这一节最后会展开讲它内部具体怎么组织、怎么挑选）里，按一定策略挑出的一个"目前评估分数还不错、值得在它基础上继续改"的候选程序。LLM接下来要做的修改，就是针对这个父程序进行的
- **灵感程序(inspirations)**：除了父程序之外，另外挑出的几个**其他**候选程序（可能来自种群里其他分支，即演化路径上不同的候选分支），提供给LLM作为"可以参考、借鉴思路"的额外样本——不是要在它们身上做修改，只是让LLM在生成新想法时，能看到"还有哪些不同的解法思路曾经被尝试过"，避免每次都只在同一个方向上小修小补

**Prompt sampler具体怎么把这些拼装成一个prompt，这个prompt的作用是什么**：Prompt sampler做的事情就是把下面这几类信息组织进同一段文本，作为喂给LLM的输入：
1. 父程序的完整代码（尤其是标记为"EVOLVE-BLOCK"、允许被修改的那部分）
2. 灵感程序的代码片段（作为"这是另一种曾被尝试过的思路"的参考）
3. 父程序**上一次被评估时**的分数和具体反馈（比如"这个方案在测试用例A上跑出了0.82分，在测试用例B上失败了，原因是XX"）
4. 一段固定的系统指令，告诉LLM"任务是提出一个能提升评估分数的代码修改，请用SEARCH/REPLACE的格式给出diff"
5. （可选）人工提供的背景知识，比如相关的公式、文献片段、或者对问题的补充说明

这个prompt的作用很直接：**让LLM不是凭空生成代码，而是"看着已有的最佳成果、看着它的具体短板在哪、参考别的思路"去做一次有依据的改进尝试**——这跟直接让LLM"写个调度算法"效果完全不同，后者每次都是从零开始瞎猜，前者是站在已有进展上做定向优化，加上"评估反馈"这个信息，相当于告诉LLM"上次这么改是错的/不够好，别再犯同样的问题"。

- **LLMs ensemble（LLM集成）**：这里"ensemble"就是字面意思——**不是单一模型**，是同时使用多个不同的LLM（论文里提到用了Gemini的不同型号，一个更快但能力稍弱的模型负责生成大量候选，一个更慢但更强的模型负责生成更高质量的关键改动，兼顾速度和质量）。这些LLM读入上面Prompt sampler拼装好的prompt，输出的不是整段重写的代码，而是**diff（差异补丁）**——具体格式是类似git合并冲突标记的"SEARCH/REPLACE"块：论文原文prompt里LLM输出形如
```
<<<<<<< SEARCH
（要被替换的原始代码片段）
=======
（替换后的新代码片段）
>>>>>>> REPLACE
```
这样的一小段修改指令，只改代码里被标记为"EVOLVE-BLOCK"（可进化区域）的部分，其余代码保持不动——这也是它能处理"整份代码文件、几百行"规模的关键：不需要每次都重新生成整个程序，只需要生成一个小修改

- **Evaluators pool（评估器池）**：负责给LLM刚生成的这个新候选程序打分。评估器不是AI模型，是用户自己写的可执行评估代码——这是搭建这整套系统里工作量最大、最需要针对具体任务设计的一环，紧接着这一节会用整段篇幅拆解它具体怎么构建

- **Program database（程序数据库）**：存储所有已经生成并评估过的候选程序及其分数，供Prompt sampler下一轮挑选父程序/灵感程序时取用。它内部具体怎么组织候选（会直接决定"父程序该选哪个、灵感程序该选哪些"这两个问题的答案），在下面"种群"部分展开

循环方式（论文里给的伪代码逻辑）：`父程序,灵感程序 = 数据库.采样() → prompt = 提示采样器.构建(父程序,灵感程序,历史评分) → diff = LLM.生成(prompt) → 新程序 = 应用diff(父程序) → 分数 = 评估器(新程序) → 数据库.注册(新程序,分数)` ——然后回到采样，如此循环。可以看到，"父程序/灵感程序怎么被挑出来"和"新程序算出的分数怎么被存回去、影响下一轮的挑选"，这两件事都是Program database在管理，这就是为什么下一部分要专门讲它内部的组织方式("种群"和进化算法)。

### "自动评估器"具体怎么构建——这确实是最费工夫的一环，逐步拆解

先明确一点：评估器**不是一个训练出来的AI模型**，不需要额外"训练"什么东西，本质上是**为具体任务手写的一段普通代码**（一个函数），只是这段代码要能把"这个候选方案好不好"转换成一个数字。构建它一般包含以下几步，具体工作量因任务而异，但思路是通用的：

1. **先想清楚"好"是什么，把它拆成可计算的指标**：比如任务是"设计一个调度算法"，"好"具体可以拆成"资源利用率高"、"任务等待时间短"这类可以直接算出数值的指标；任务是"发现一个数学构造"，"好"就是"是否满足给定的数学约束条件"（这种情况下甚至不需要"打分"，只需要"对/错"的二元判断）
2. **搭建一个能自动跑候选方案的执行环境**：比如调度算法这个例子，需要有一段模拟代码，能把候选算法接入一个模拟的任务分配场景，跑完一轮分配后统计出资源利用率这类数字。这一步的工作量确实不小——需要为具体问题准备好"跑一次实验/模拟需要的全部环境"，这部分工作跟日常做实验验证一个想法时要搭建的实验流程是同一件事，只是现在要让它能被代码反复自动调用，不需要人工介入
3. **把第1步的指标和第2步的执行结果串起来，写成一个函数**：输入是候选程序，函数内部调用第2步的执行环境跑一遍，把跑出来的原始结果，按第1步定义的方式转换成一组标量分数返回
4. **（可选）针对论文提到的几个工程痛点做优化**：
   - **分级评估（evaluation cascade）**：如果完整评估一次很贵（比如要重新跑一次完整模拟/训练），可以先设计一个"简化、快速版"的检查，比如先跑一个小规模测试，明显不合格的候选直接被淘汰，不用送去做完整昂贵评估——这样能省下大量计算，把资源留给真正有希望的候选
   - **LLM辅助打分作为补充**：如果某个质量维度确实很难写成精确的数值公式（比如"这段代码写得简洁不啰嗦"），可以额外调用一次LLM，让它对这个维度打分，作为补充分数——这是"手写的确定性评估代码"和"临时借用AI当裁判"两种手段的混合使用，不是必须二选一
   - **并行评估**：如果单次评估很耗时（论文提到有些场景一次评估要花上百个计算小时），就需要让评估器池支持同时评估多个候选，不然新候选生成的速度会被"排队等评估"卡住

所以"构建评估器复杂不复杂"这个问题，答案是：**复杂程度基本等同于为这个任务搭建一套自动化实验/验证流程的复杂程度**，AlphaEvolve没有省掉这一步，它省掉的是"想出下一个候选方案该怎么改"这一步（交给LLM），而"怎么判断一个方案好不好"这个领域知识，仍然需要人来提供。这也解释了为什么AlphaEvolve适用的问题都有一个共同特点——论文强调的"machine-gradeable solutions"（候选方案可以被机器自动评判的问题），如果一个问题的"好坏"本身就很难量化（比如"写一篇好文章"），这套系统就没有合适的评估器可用，用不上。

### "种群"和"进化算法"具体指什么——澄清"是不是遗传算法"

**种群(population)**：就是"目前已经生成、并且评估分数还不错、值得继续在此基础上改进"的一批候选程序的集合，存放在Program database里，也就是上面"父程序/灵感程序"是从中挑选的那个池子。

**这套机制具体是不是遗传算法**：论文明确说，Program database采用的算法"受到MAP-Elites算法和基于岛屿的种群模型（island-based population models）启发的组合"——这确实是进化计算/遗传算法这个大家族里的技术，但不是最简单版本的遗传算法（简单遗传算法通常是"整个种群一起淘汰排序，保留分数最高的一批，其余全部淘汰"）。AlphaEvolve用的这两个技巧解决的是同一个核心难题——**怎么在"专注打磨当前最优解"和"保留多样性、探索其他可能更优的方向"之间做平衡**（论文原文用词是exploration和exploitation的平衡）：
- **MAP-Elites**：不是只保留"总分最高的那一个"，而是把候选按照几个不同的特征维度分到不同的"格子(cell)"里，每个格子分别保留该格子内最优的候选——这样即使某类候选当前总分不是最高，只要它在某个维度上有特色，也会被保留下来，不会被过早淘汰，保住了多样性
- **岛屿模型(island-based population models)**：把种群拆分成多个相对独立的"岛屿(island)"子种群，各岛屿内部独立演化一段时间，偶尔才在岛屿之间交换个别优秀候选（这个操作叫"迁移"）——好处是不同岛屿可能各自演化出不同的解决思路，不会因为某一条思路暂时领先就让全局种群都收敛到它，减少"过早收敛到局部最优"的风险

所以更准确的说法是：AlphaEvolve用的是"启发于遗传算法思想、但专门为'保持解法多样性、避免过早收敛'做了改良"的一种进化算法，不是教科书里最基础的那种"排序淘汰"式遗传算法——它确实属于遗传算法这个大家族，只是具体机制比最简单版本更精细。

### 三个具体落地案例的技术细节

- **数据中心调度算法**：Google数据中心用一套叫Borg的系统做集群调度——把待运行的计算任务分配到机器上。这个问题的痛点叫"资源碎片化(stranded resources)"：一台机器可能内存已经分配满了、但CPU还空着很多，这部分空闲CPU由于配套的内存不够，没法再塞进新任务，等于被"卡住"浪费掉。AlphaEvolve做的事情是：把这个问题形式化成一个"打分函数"——给定一个任务的资源需求和一台机器当前的剩余资源，这个函数输出一个分数，调度器据此决定把任务分给哪台机器。论文里实际给出了AlphaEvolve发现的公式（写成代码大意）：
```
cpu_residual = 任务需要的CPU / 机器剩余CPU
mem_residual = 任务需要的内存 / 机器剩余内存
score = −(cpu_residual + mem_residual + mem_residual/cpu_residual + cpu_residual/mem_residual)
```
这个公式里后两项(交叉比值)是关键——它不只是简单看"这台机器剩余资源够不够用"，还惩罚"两种资源使用比例严重不均衡"的分配方式(比如CPU几乎用满但内存还剩很多，或反过来)，从而系统性地减少资源碎片化。这个公式已经实际部署进Google生产环境的调度系统里

- **硬件电路简化**：目标是优化TPU矩阵乘法单元里一个已经被人类工程师高度优化过的Verilog电路描述(RTL，Register-Transfer Level，芯片设计中用来描述硬件行为的抽象层级)，要求在保持功能完全正确的前提下减少芯片面积和功耗。AlphaEvolve找到的改动是一处"去掉了多余比特位"的简化写法——这个改动必须先过硬件验证工具的正确性检验才能被采纳（这正是"确定性评估器"在硬件场景下的体现：不是AlphaEvolve自己说"这样改是对的"，而是有专门的电路验证工具做客观检查）。这个改动后来被集成进了下一代TPU，是论文强调的一个"直接影响真实芯片"的案例

- **自我加速**：论文提到AlphaEvolve被用来优化了训练"AlphaEvolve自身所依赖的那个LLM"过程中的某个计算环节（矩阵乘法kernel，即训练大模型时反复被调用、对速度影响很大的底层计算函数）——这构成一个具体的"自举"闭环：用AlphaEvolve去改进它赖以运行的那个LLM的训练效率，效率提升后，理论上下一次跑AlphaEvolve时能获得更快/更强的LLM去做进化搜索

- **数学发现（矩阵乘法算法）**：这是论文最受关注的结果之一——AlphaEvolve找到了一种用48次标量乘法完成两个4×4复数矩阵相乘的算法，比经典的Strassen算法（1969年提出，是矩阵乘法领域的里程碑工作）在这个具体规模和数域下的乘法次数还要少，是56年来在这个特定问题上的第一次改进。这类问题的"评估器"就是直接的代数验证：任何声称的乘法方案，都可以通过纯符号计算严格验证它是否真的等价于矩阵乘法，属于完全确定性、无歧义的验证——这也是为什么这类数学发现类结果格外有说服力：不存在"看起来对但实际有漏洞"的可能性，一旦通过验证就是严格成立的新知识

### 和FunSearch的关系（论文给出的具体对比表格）

AlphaEvolve是DeepMind更早工作FunSearch（同样是"LLM+进化搜索+自动评估"发现新数学构造/启发式算法）的扩展和泛化，论文给出了逐项对比：

| 维度 | FunSearch | AlphaEvolve |
|---|---|---|
| 进化对象 | 单个函数 | 整份代码文件（可以涵盖多个函数/组件） |
| 单次修改规模 | 最多10-20行 | 最多几百行 |
| 支持语言 | 仅Python | 任意语言 |
| 评估耗时要求 | 需要很快（单CPU约20分钟内） | 可以耗时数小时，且支持并行、可用加速器 |
| 所需LLM采样量 | 需要数百万次采样 | 数千次采样就够 |
| 对更强LLM的收益 | 用小模型即可，换更强模型没有额外收益 | 明显受益于更强的SOTA LLM |
| Prompt上下文 | 极简（只有历史解） | 丰富的上下文和反馈 |
| 优化目标数 | 单一指标 | 可同时优化多个指标 |

一句话总结这个对比：FunSearch证明了"LLM+进化搜索"这个思路本身可行，但局限在小规模、单指标、需要海量采样弥补LLM能力不足；AlphaEvolve则是把这套思路扩展到能处理复杂真实系统的规模，代价是需要更强的LLM，好处是采样量反而大幅减少（好的LLM一次提出的修改质量更高，不需要靠数量堆概率）。

---

## 11. 具身智能自迭代 —— 补充细节

在讲四篇论文之前，先补一层背景知识——具身智能/机器人学习里几个反复出现的基础概念，不然"策略"、"rollout"、"状态"这些词在具体机制里会不知道指的是什么。

### 具身智能背景知识：策略、状态、rollout分别指什么

**策略(policy)**：这是强化学习里的核心概念（第4节PPO部分提到过一次，这里展开讲）。"策略"可以理解成"一个决策函数"——输入是机器人当前观察到的信息（摄像头画面、机械臂当前姿态角度等，这些信息合起来叫"状态(state)"或"观测(observation)"），输出是"接下来要执行的动作"（比如机械臂各关节应该转动到什么角度，夹爪应该张开还是闭合）。在具身智能语境下，"策略"和"模型"经常是同一个东西——策略通常就是一个神经网络（可能是第10节提到的VLA模型，也可能是别的架构），"训练策略"、"改进策略"、"训练模型"在这个语境下基本是同义词，都是指调整这个决策网络的参数，让它输出的动作序列更能完成任务。

**Rollout（第7节提到过这个词，这里放到具身场景里再确认一次）**：指"让当前策略完整执行一次任务，从头到尾"——比如让机器人从初始状态开始，按策略输出的动作一步步执行，直到任务完成或失败，这个完整的过程和记录下来的数据（每一步的状态、动作、是否成功等）就叫一次rollout。

**行为克隆(behavior cloning)**：这是训练策略最直接的一种方式——收集一批"人类操作员示范的成功轨迹"（状态-动作对），然后让神经网络去模仿这些示范，本质上是一种监督学习（跟第2节说的"下一词预测"是同一套思路，只是这里预测的不是下一个词，而是给定当前状态该输出什么动作）。它的局限是"只能学到示范里出现过的情况"，遇到示范没覆盖到的新状态容易表现不好。

**离线/在线强化学习(offline/online RL)**：都是用"奖励信号"而不是"直接模仿示范"来训练策略的方式（RL的基本逻辑在第4节PPO部分讲过）。"在线"指训练过程中策略要不断和环境（真实机器人或仿真器）实时交互、收集新数据；"离线"指只用一批已经收集好、不再变化的历史数据来训练，不需要在训练过程中继续和环境交互——离线RL的好处是不需要一直占用机器人做训练，但效果通常不如在线RL灵活。

**启发式规则学习(heuristic learning)/工具调用(tool calling)**：这两个是相对没那么"机器学习"、更偏工程手段的策略改进方式——启发式规则学习指人工/agent直接设计一套基于规则的判断逻辑（比如"如果检测到位置偏差超过X，就往这个方向修正Y距离"），不一定需要神经网络；工具调用指策略在执行时可以调用外部现成的工具/子程序（比如调用一个专门的运动规划库来算一条避障路径），而不是完全依赖神经网络自己算出每一步动作。这几种手段在ENPIRE里是可以互相组合使用的，不是互斥的选项。

有了这些背景，下面四篇论文的机制就可以逐一讲清楚了。

### ENPIRE：四模块具体是什么、怎么循环起来的

ENPIRE的四个模块不是"各自独立做一件事"，而是**分两大阶段构建、构建完之后循环运行**，论文原文明确分成"第一阶段：环境构建"和"第二阶段：自动化策略改进"，下面按这个结构展开。

**第一阶段：环境构建（EN模块 + R模块的一部分，一次性搭好，后面反复复用）**

这一阶段的目标是把"物理世界的交互"包装成一套agent能读懂、能自动操作的接口，包含三件事：
1. **硬安全约束**：限制机器人的动作范围和运动方式在一个安全区间内，一旦超出这个区间就立即判定任务失败并自动触发重置——这既是安全保护，也顺带提供了一种"任务什么时候算结束"的判定方式
2. **自动验证(automated verification)**：机器人做完一次尝试后，怎么知道这次是成功还是失败，不能每次都靠人看。做法是：先让人类提供几分钟的"成功"和"失败"示范录像，coding agent看着这些示范视频和机械臂姿态数据，自己写代码合成出一个"二元奖励函数"（输出成功/失败的判断），目标是让这个函数的判断准确率高、判断速度快。比如插销任务，agent自动发现一个基于"视觉对齐程度+末端执行器高度+力传感器读数"组合出的判断规则来确定插销是否插到位
3. **自动重置**：机器人失败或完成一次尝试后，需要自动恢复到初始状态才能开始下一次尝试，不需要人工去摆放场景

这三件事合起来构成**Environment模块(EN)**。另外，策略执行时接收的"视觉+机械臂姿态"传感器输入、以及策略输出后提交给机器人控制器的动作指令，加上刚才那个自动验证给出的奖励，这一整套"策略和物理世界打交道的接口"构成**Rollout模块(R)**——所以R模块准确说是"策略和真实机器人交互、拿到数据的通道"，不是一个独立的算法模块。

这一阶段需要人类介入一次（帮agent确认环境接口搭得对不对），之后就是一次性成本，可以在后续所有策略训练里反复复用，不需要每次都重新搭。

**第二阶段：自动化策略改进（PI模块 + E模块，反复循环运行）**

有了第一阶段搭好的环境接口，coding agent现在的目标变成"通过自主实验，最大化任务成功率"，做法是：
1. agent被赋予对训练代码库的写权限（可以直接修改训练代码，不是只能调参）
2. agent通过**Policy Improvement模块(PI)**：查阅文献产生思路、提出假设、然后**直接修改训练代码本身**——比如把训练算法从行为克隆换成某种RL算法，或者调整RL的某个超参数/奖励设计。这一步用到的具体技术手段就是上面背景知识里讲的那几种（启发式规则、工具调用、行为克隆、离线/在线RL），agent根据任务情况自己选或换
3. agent通过R模块在真实机器人上跑这个新策略（一次rollout或多次），拿到轨迹、录像、奖励信号这些"证据"
4. agent检查这些证据，判断这次修改有没有让成功率提升，决定下一步怎么改——这个"分析证据、决定下一步方向"的过程，如果是在**多机器人并行**的场景下做，就升级成了**Evolution模块(E)**：多个agent同时分布在多台机器人上，各自独立测试不同的假设/训练配方，agent之间通过Git协作，互相cherry-pick（挑选）、复制或合并队友那边跑出来的成功训练配方——也就是说，E模块的"进化"，指的是**多个并行分支的训练配方之间互相借鉴、优胜劣汰**，而不是"改进研究过程"这种抽象说法

**四模块怎么循环起来（完整说一遍）**：
EN模块提供安全约束+自动验证+自动重置的环境接口(一次性搭好) → agent通过R模块在这个环境里跑策略、拿到轨迹和奖励数据 → agent通过PI模块分析这些数据、直接修改训练代码提出新的策略改进方案 → 用改进后的策略重新跑R模块 → 如果是多机器人并行，各分支的进展通过E模块互相借鉴融合 → 循环回到R模块继续跑，直到成功率达标或迭代预算用完。

用一支8站双臂机器人机队(YAM)并行运行这整套循环（多个agent、多台机器人同时跑，对应上面的E模块），在PushT、整理别针、用剪刀剪扎带等灵巧操作任务上做到99%成功率。核心论点：真实世界机器人研究自动化真正缺的一环，就是这样一个"可重复的物理反馈循环"（能自动判断成功/失败、能自动重置），而不是缺更好的算法或更强的模型能力——这也是为什么论文把大量篇幅放在"怎么搭建自动验证、自动重置"这个看似基础的工程问题上。

### Criticality Model：具体机制、训练方式、以及部署后的作用

先解释两个术语：
- **VLA模型（Vision-Language-Action model，视觉-语言-动作模型）**：具身智能领域一类模型的统称，输入摄像头画面(vision)和/或语言指令(language)，直接输出机器人的动作指令(action)，是当前机器人操作任务里的主流模型架构，可以类比成"给机器人用的、输出是动作而不是文字的LLM"——上面ENPIRE背景知识里说的"策略"，在这类工作里往往就是一个VLA模型
- **重要性采样(importance sampling)**：一种统计上的采样技巧。正常情况下应该按真实分布(均匀随机)采样才能保证训练目标"无偏"（unbiased，指估计出的期望值不会系统性地偏离真实值）；但如果故意更多地采样"更有价值/更少见"的样本(比如这里的高失败风险场景)，会让采样分布偏离真实分布，直接训练会产生偏差。解决办法是给每个样本乘上一个"重要性权重"来抵消这种偏差——这样既能把训练的注意力集中到关键样本上，又不会让最终的训练目标产生系统性偏移。这不是这篇论文独创的技巧，是统计学和强化学习里的通用手段。

**问题**：默认的数据采集是均匀随机的，导致训练集被"正常情况"淹没，真正有价值的"罕见失败场景"占比很低，信息密度不足。

**关键性模型c(s)具体怎么训练**：这是一个轻量级的二分类神经网络，输入是状态s，输出一个0到1之间的数，代表"预测这个状态未来会走向失败的概率"P(failure|state)。训练数据从哪来：让当前策略先正常地跑一批rollout(在任务相关的扰动下反复执行)，每次rollout结束后自然会知道这次是成功还是失败(标签y，成功=0失败=1)，这样就得到了一批(状态,是否失败)的数据对，直接用标准的二元交叉熵损失训练c(s)——跟常见的分类网络训练完全是同一套流程，不需要额外的人工标注，标签就是"这次rollout最后成功没成功"这个自带的结果。

**用关键性分数具体怎么改变采样**：训练时不再是均匀随机抽取过去收集的状态，而是构造一个新的采样分布q(s)，让c(s)分数高(容易失败)的状态被抽到的概率更大——论文里实际用的是把c(s)做一个指数放大(比如exp(3×c(s)))来进一步拉大高/低关键性状态被抽到的概率差距。每个被抽到的样本会附带一个"重要性权重"w = 真实概率/被抽到的概率——关键性越高的状态，虽然被更频繁地抽到，但对应的权重会被相应调小；关键性低、被采样概率也低的状态，权重会被调大来补偿。这样加权之后，整体的训练目标在数学期望上仍然等价于"按真实分布训练"，不会因为"故意多看几眼失败场景"而让模型产生偏差性的错误认知，只是让稀缺、有价值的样本在训练时获得了更高的有效利用率。

**训练循环的三个阶段**（论文原文Figure 1的三阶段）：Stage 1，用当前策略做一批rollout，根据成功/失败结果训练关键性模型c(s)；Stage 2，用c(s)构造上面说的重要性采样分布，引导后续的数据采集更多地覆盖高失败风险的状态；Stage 3，策略在这批经过重要性采样、权重校正后的数据上做微调。Stage 2和3反复迭代，直到失败率下降到饱和(继续迭代也没有明显提升)为止。

**部署阶段的额外用处**：训练循环结束后，c(s)不是被丢弃的中间产物，而是被继续留用当一个"风险监控器"——机器人实际运行时，c(s)持续监控当前状态，一旦c(s)超过某个阈值(判断当前处境比较危险/容易失败)，就自动把控制权切换到经过重点强化训练的那个策略版本上，相当于c(s)身兼"训练时的采样引导"和"部署时的实时风险预警"两个角色。

效果：在四足运动、多任务操作、VLA模型、真实机器人任务上，相对基线降低失败率51%-67%，相对当前最强VLA模型降低8%-25%（论文额外验证了：如果只是单纯用随机采样的数据继续训练、不用关键性模型引导，几乎没有提升，证明效果确实来自"关键性引导的采样"，不是单纯"多训练了几轮"）。

### AGT-World：affordance图具体怎么构建、自我进化具体怎么运作

先解释"affordance"：这是认知科学/机器人学里的一个概念，指"一个物体本身所暗示的、可以对它执行的操作"——比如一个杯子的affordance包括"可以被抓握的把手"、"可以被倾倒的开口"；一个门把手的affordance是"可以被旋转/按压"。

**整个流程分三步，第一步是把真实图片变成可交互仿真场景**：给定一张真实场景的RGB图片，用已有的场景重建方法（论文引用了别人的重建框架）把这张图重建成一个仿真场景，同时保留物体的语义信息(这是什么物体)和affordance信息(这个物体能被怎么操作)——这一步解决的是"仿真场景要和真实世界的物体状态、可操作性对得上"这个问题，而不是靠视频生成模型直接生成一个画面好看但物理上乱来的场景(论文明确指出纯视频生成模型的问题就是"物理上不合理")。

**第二步是把任务空间表示成一个图，用于规划任务**：论文把整个任务空间形式化成一个有向图G=(V,E)——图里的节点代表"物体+可能的动作组合"构成的一种离散配置状态，边代表从一个配置转移到另一个配置需要执行的动作。这样"生成一个任务"就变成了"在这个图上做路径规划"（从当前状态节点，规划一条到目标状态节点的路径），而不是像很多方法那样随机地凭空想一个任务出来——这保证了生成出来的任务序列在逻辑上是连贯的（论文提到这解决了"随机生成的场景缺乏真实感、RL在这类场景下奖励信号过于稀疏"的问题）。具体执行动作时，用一个LLM把每个子任务描述映射成一个由**21种"原子操作"（atomic primitives）**组成的动作序列——这21种原子操作覆盖导航、机身移动、末端执行器(机械臂/夹爪)操作等基本动作类别，是这个系统里"最小的、不可再分的动作单元"，任何复杂任务最终都要被LLM拆解成这21种操作的某种组合排列。

**第三步是自我进化机制，具体运作方式**：执行完一个子任务的动作序列后，系统会抽取执行过程的多视角视频帧（多个摄像头角度）作为反馈。一个VLM（视觉语言模型，只做理解和输出文字判断，不直接输出动作，跟上面VLA模型的关键区别）依次观看这些视频帧，对每一步动作给出一段"语义批评"(semantic critique)——即用自然语言描述这一步执行得怎么样、哪里有问题(比如指出"机械臂在移动时蹭到了旁边的物体边缘"这类问题)。需要澄清的一点是：论文摘要里提到的"几何验证"并不是一个独立于VLM之外的几何计算模块，实际机制是VLM本身对视频的语义理解同时覆盖了语义层面(有没有拿对物体)和几何/空间层面(有没有撞到东西、角度对不对)的问题，是同一套判断在起作用，不是两个分开的子系统。所有这些逐步的批评意见，加上完整的历史视觉数据和任务上下文，一起被用来提出"下一轮迭代"的策略修正方案(比如调整某个动作的移动距离参数)——这就是"提出任务→执行→纠正"这个自我改进循环里"纠正"这一步的具体内容。

效果：在102个自动生成的场景-任务对上，达到71.6%的整体成功率；论文也在4个复杂长程任务上验证了这个自我进化机制确实能有效纠正执行错误。

### RISE：组合式世界模型具体由什么组成、想象空间训练具体怎么运作

先解释两个术语：
- **世界模型(world model)**：一个学习出来的、能模拟"如果执行这个动作，环境接下来会变成什么样"的模型——相当于给策略一个"内部沙盘"，可以在不真的执行动作的情况下，预测执行后的结果
- **advantage（优势，第4节PPO部分提到过）**：实际获得的奖励减去"预期奖励"的差值，用来判断"这个动作比平均水平好还是差"，是RL里衡量"要不要提高这个动作的概率"的核心量

**"组合式(compositional)"具体指什么**：不是一个笼统的黑箱模型，而是明确拆成两个职责不同、架构也不同的子模块，下面逐个讲清楚它们的输入输出格式、内部结构、以及怎么训练出来的。

#### 动态模型(dynamics model)：这个"内部沙盘"具体长什么样、输出什么、怎么训练

**输入输出的精确格式**：设当前时刻t，机器人有n个摄像头，t时刻的观测记作o_t=[m_t1,...,m_tn]（n张不同视角的画面）。为了让模型知道"最近发生了什么"，会往前回看N步，构成一个观测窗口O_t={o_{t-N},...,o_t}。策略接下来打算执行的不是一步动作，而是一次性给出连续H步的"动作块(action chunk)"a_t=[a_t,a_{t+1},...,a_{t+H-1}]。动态模型D的任务就是：**输入这个观测窗口O_t和这个动作块**，**输出接下来H步、每一步n个视角的画面预测**{ô_{t+1},...,ô_{t+H}}——也就是说，这个"内部沙盘"输出的数据结构和输入是同一类东西：都是多视角的图像/视频帧，不是什么抽象的向量或符号表示，可以直接把输出理解成"如果这样做接下来会拍到的视频"，只是这段视频是模型生成出来的，不是真实拍摄的。

**内部结构和是怎么训练出来的**：动态模型不是从零开始训练的，而是在一个已有的、专门做视频生成的预训练模型（论文用的是"Genie Envisioner"这个视频生成模型的一个变体，其架构又是继承自另一个视频生成模型LTX-Video）基础上继续训练得到的——这类模型的通用工作原理属于"视频扩散模型"：本质上是学会"从一堆随机噪声出发，逐步去噪、还原出一段逼真的视频"，训练时的做法是拿真实视频加上不同程度的噪声，让网络学会"预测噪声、把它去掉"，训练好之后，给定一些起始条件（这里就是历史观测帧+动作块），就能从噪声出发"生成"出符合条件的未来帧，这跟Stable Diffusion一类图像生成模型是同一套技术思路，只是这里生成的是连续的视频帧，而且额外要求生成的内容要跟给定的动作块保持物理上的一致（做了这个动作之后，画面里的物体确实要按这个动作该有的方式移动，不能乱动）。

具体的训练数据和训练技巧：
- **预训练**：先在大规模机器人数据集(论文提到用了Agibot World、Galaxea这类公开数据集)上预训练，学会"给定观测和动作，生成合理的未来视频"这个基本能力，训练时额外加入一个轻量的动作编码器，把动作信息也编码进网络能理解的形式
- **一个工程细节(Task-Centric Batching)**：如果直接混合各种不同任务、不同场景的数据一起训练，容易训练不稳定、收敛慢。论文的做法是：每个训练批次(batch)只从少数几个任务里取样，但同一个任务下尽量覆盖更多"不同动作导致不同结果"的样本——直觉是优先让模型学"同一个场景下，不同动作会导致什么不同结果"（这才是"动作可控性"真正需要学的东西），而不是让模型在"到底是哪个任务/场景"这个更粗糙的区分上花费学习容量
- 最终得到的动态模型要同时满足两个要求：生成速度不能太慢(否则会拖慢整个RL训练的节奏)，生成内容要既"看起来逼真"又"确实符合动作指令"(不能画面好看但物体乱飘)

#### 价值模型(progress value model)：怎么把"想象出来的画面"转换成一个能指导训练的分数

**输入输出**：输入是动态模型刚刚"想象"出来的某一步观测(o_t)和当前任务的语言指令(l)，输出一个标量分数V(o_t,l)，代表"看这张想象出来的画面，这个任务目前完成到什么程度了"。

**从哪个模型初始化、为什么这么选**：价值模型不是随机初始化训练的，而是从一个预训练好的VLA策略模型（论文用的是π0.5这个具体的VLA模型）初始化而来——好处是这个VLA骨干网络已经具备理解"机器人视角画面+语言指令"的基础能力，不需要从零学"看懂图像里在做什么"这件事，只需要在这个基础上再学会"打分"这一层新任务。

**具体训练目标(两个loss叠加)**：
1. **进度回归损失(progress regression)**：给定一段已知的、完整的执行轨迹（长度为T步），直接用"当前是第t步"这个信息作为一个粗略的监督信号，让V(o_t,l)去逼近t/T这个比例值(比如整段轨迹100步，当前第30步，就让V尽量输出0.3)——直觉很简单：越接近任务结束，分数应该越高。这个信号的好处是"处处都有"（每一步都能算），缺点是太"平滑"、不敏感，任务执行到一半突然出了个小错误，这种loss不容易捕捉到
2. **时序差分学习(Temporal-Difference learning, TD learning)**：这是标准强化学习里估计价值函数的经典方法，思路是让V(o_t,l)去逼近"这一步的即时奖励 + 打折后的下一步价值V(o_{t+1},l)"，其中"即时奖励"在中间步骤设为0，只在**一整段轨迹结束时**给出+1(成功)或-1(失败)——这样即使中间过程看起来很平滑正常，只要最终失败了，这个"失败"的信号会通过TD更新逐步"倒灌"回前面的步骤，让V学会识别出"看似正常但实际正在走向失败"的细微线索，这正好补上了第一个loss不敏感的短板
3. **两个loss直接相加**作为价值模型的最终训练目标——一个提供密集但迟钝的信号，一个提供稀疏但敏锐的信号，合起来既稳定又能捕捉细微失误

这两个模型分别训练好之后，串起来使用：动态模型想象出一系列候选动作对应的未来画面，价值模型给这些想象出的画面打分，分数经过换算（减去某种预期基线）变成advantage，供策略训练使用——这个"用advantage指导策略更新"的逻辑，跟第4节PPO里讲的"优势"用法是同一套思路，只是这里的优势来自对**想象出来的**画面打分，不是来自真实执行的结果。

"组合式"的好处：动态模型和价值模型可以用最适合各自任务的不同预训练起点、不同架构分别设计和训练(动态模型继承自视频生成模型、擅长生成逼真连贯的画面；价值模型继承自VLA模型、擅长理解画面语义并打分)，不需要用同一个模型同时兼顾"生成"和"评分"两件事，这两件事对模型能力的要求本来就不太一样。

**整体训练分三个阶段（论文Section III的结构）**：
1. 先单独训练好组合式世界模型(动态模型+价值模型)本身，让它能准确模拟"动作→未来画面"和"画面→进度评分"这两件事
2. **策略热身(Policy Warm-up)阶段**：用真实世界的历史数据先训练一版基础策略，把它"锚定"在真实可行的动作分布上，并让它具备"能根据advantage调整行为"的能力——这一步是必要的，直接让一个没见过真实数据的策略在纯想象空间里学习容易学到不切实际的行为
3. **自我改进循环(Self-Improving Loop)**：反复交替执行"Rollout阶段"和"Training阶段"——Rollout阶段：从一个采样的初始状态开始，用当前策略在**世界模型内部**（不是真实机器人上）生成一整段"想象中的"执行过程，动态模型预测每一步的画面，价值模型给每一步打分算出advantage；Training阶段：用这些完全在想象空间里生成的、带advantage标注的数据去更新策略参数。这个循环可以在纯计算(不占用真实机器人)里反复跑很多轮，实现"大量想象rollout"来自举策略性能

跟ENPIRE(强调必须在真实机器人上跑闭环)、AGT-World(强调仿真环境要尽量物理真实)相比，RISE的路线更极端——直接在一个学习出来的、纯神经网络模拟的"内部世界"里做几乎全部的试错和训练，只在最开始的"策略热身"阶段依赖一批真实数据，这是三条技术路线里对真实世界物理交互依赖最少的一个，好处是几乎不消耗真实机器人的时间和硬件损耗，代价是完全依赖世界模型模拟得够不够准，一旦世界模型的预测和真实物理规律有系统性偏差，学到的策略可能在想象空间里表现很好但真实执行时不奏效。

### 四篇工作合起来暴露出的挑战，以及为什么具身智能的"开放式RSI"会比语言模型更晚到达

把上面四篇工作放在一起看，能看出具身智能领域自迭代目前几个尚未解决的结构性问题，这些问题不是某一篇论文的局限，是整个领域共同面对的：

1. **安全性是语言任务完全不需要考虑的一个维度**：语言模型生成一个错误答案，代价是"输出一段没用的文字"，可以直接丢弃重试；机器人策略在真实世界试错，代价可能是损坏硬件、损坏环境物品，极端情况下还有安全风险。这意味着具身智能的闭环自迭代不能像Absolute Zero那样"完全自由地随机探索"，探索空间必须被安全约束限制住，这本身就压缩了"开放式"的程度。

2. **验证信号看似客观，但没有想象中那么"硬"**：物理结果的对错表面上是客观的（箱子到底有没有推到指定位置，摄像头/传感器能直接判断），这一点确实比语言任务的"回答好不好"更接近第0节排序里的"形式化验证器"这一档。但深挖一层会发现：验证器只能检查"最终结果"，检查不了"过程是否合理"——一个策略完全可能靠"钻验证规则的空子"拿到高分(比如利用仿真器里的物理引擎bug、或者用一种不安全但恰好能让传感器读数达标的方式完成任务)，这跟第0节提到的reward hacking是同一类问题，只是发生在物理/仿真层面而不是语言层面。所以具身场景的验证器实际介于"形式化验证"和"过程裁判"之间，没有想象中那么绝对可信。

3. **仿真-真实差距（sim-to-real gap）**：这是具身智能特有的问题，语言模型不存在这个问题（语言任务的"仿真"和"真实"是同一个东西，都是文本）。AGT-World试图靠"用真实观测重建结构化仿真环境+几何验证"来缩小这个差距，但视觉逼真度（画面看起来像不像真实世界）和物理精确性（物理引擎算得准不准）这两者在计算成本上往往是要权衡取舍的——追求两者都完美会让仿真跑得很慢，所以纯仿真里学到的自我改进，未必能可靠迁移到真实机器人上，这也是为什么ENPIRE要坚持"直接在真实机器人上"闭环，而不是完全依赖仿真。

4. **数据吞吐天花板远低于语言任务**：语言模型的"生成"是纯计算，一台服务器理论上能并行生成海量文本样本；机器人的"生成"是物理动作执行，受限于硬件数量、每次执行的真实耗时、机械磨损等物理约束，不可能像文本生成那样无限并行扩展。ENPIRE用多机器人机队并行是在硬件层面对抗这个限制，但天花板依然远低于纯文本场景。

**为什么这意味着具身智能会更晚接近"开放式RSI"（第7节谱系的右端）**：回顾第7节提到的开放式RSI三大瓶颈——接地要求、模式崩溃风险、算力约束。具身智能在"接地要求"上反而是三个领域里最容易满足的（物理结果天然接地，不需要担心"虚假的自我认可"），但在"算力约束"（数据吞吐天花板）和一个语言模型不需要考虑的新维度"安全约束"上，反而比纯语言任务更受限——这是一个此消彼长的关系：具身智能不缺"验证信号从哪来"这个语言模型最头疼的问题，缺的是"怎么以可承受的代价、安全地大量生成试错数据"，这跟语言模型自迭代面临的核心瓶颈刚好互补，也是为什么具身智能领域现阶段的工作重点都在"降低试错成本"（想象空间rollout、失败驱动重采样、机队并行），而不是像BigBang/Absolute Zero那样重点在"设计更好的验证机制"上。

---

## 12. 常见问题补充说明

**自迭代模型和强化学习(RL)是什么关系？是同一个东西吗？**
不完全等同。RL是"用奖励信号优化策略"的通用框架，自迭代是"训练/反馈信号的来源从人类切换到模型自身"这一更宽泛的范式，可以在监督学习(如STaR的微调)、RL(如Absolute Zero的RLVR)、纯推理时(如Self-Refine)等不同学习范式下实现。很多前沿自迭代工作确实用RL作为底层优化算法（因为RL天然支持"奖励来自任意来源"），但两者不是包含关系而是交叉关系。

**这些方法会不会导致模型"自我崩塌"、越训越差？**
这是真实风险，综述里称为collapse dynamics。规避手段包括：(1) 保留/混入一定比例真实数据防止分布完全漂移(如SPIN的理论保证依赖p_data作为锚点)；(2) 用尽可能"硬"的验证信号(代码执行器、物理结果)而非纯模型自评；(3) held-out真实任务持续校准(BigBang的做法)；(4) 限制迭代轮数/加入早停机制。目前没有哪个方法能完全杜绝这个风险，长期开放式自迭代能否稳定运行仍是开放问题。

**具身智能和语言模型的自迭代最大的本质区别是什么？**
验证信号的性质不同——语言任务的"正确性"往往是软的、依赖模型/人类判断；具身任务的"物理结果"是硬的、客观的(箱子有没有推到指定位置)，但生成/试错的**代价**远高于语言任务(时间、硬件损耗、安全风险)，且天然存在sim-to-real差距。所以具身智能的研究重点不是"如何获得验证信号"(这部分相对更容易，物理引擎/传感器天然提供)，而是"如何降低试错代价、提高数据吞吐效率"(想象空间rollout、失败驱动重采样、机队并行都是在解这个问题)。

**如果一个新领域想引入自迭代思想，应该从哪里入手？**
通用的思路框架是：先问"这个任务有没有天然的、客观的验证器？"——很多科学/工程问题存在一个正向物理模型(forward model)，可以用来做一致性校验（比如从某种观测/测量数据反推出的结果，能否代入正向模型重新生成观测数据，误差多大）。这类"正向模型验证"就是一种相对"硬"的验证信号，类似AlphaGo Zero的规则验证器或Absolute Zero的代码执行器——如果能设计出"模型自己生成候选解 → 用正向模型验证一致性 → 把验证通过/不通过的样本反馈回训练"的闭环，就构成了一种领域内的自迭代范式，本质和STaR"生成推理链→验证答案对错→筛选后微调"是同构的。这也是判断"自迭代思想能不能用在某个具体任务上"最核心的一条准则：任务能提供多强的验证信号，生成/试错的代价有多高。
